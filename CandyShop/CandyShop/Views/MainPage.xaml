<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:CandyShop.ViewModels"
             xmlns:models="clr-namespace:CandyShop.Common"
             x:Class="CandyShop.Views.MainPage"
             x:DataType="viewModel:MainPageViewModel">

    <ScrollView>

        <CollectionView ItemsSource="{Binding Products}" HorizontalOptions="Center" MaximumWidthRequest="400">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Padding="10" Spacing="10">
                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />

                            <Label Text="{Binding Price, StringFormat='{0:C}'}" TextColor="Green"/>
                            <Label Text="{Binding Stock, StringFormat='Készlet: {0} db'}" TextColor="Blue"/>


                        </HorizontalStackLayout>
                    </DataTemplate>

                </CollectionView.ItemTemplate>
            </CollectionView>
       
    </ScrollView>

</ContentPage>
